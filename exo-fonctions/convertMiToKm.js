// Une fonction  qui fait la conversion d'une distance exprimée en Milles, en Kilomètres:

const convertMiToKm = (MiToMk) => {
  let Km = 1.60934
  return distance = MiToMk * Km
}
console.log(convertMiToKm(5))
